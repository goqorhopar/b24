# АВТОМАТИЧЕСКИЙ ДЕПЛОЙ БОТА

## 🤖 БЕЗ УЧАСТИЯ ПОЛЬЗОВАТЕЛЯ!

### Вариант 1: Systemd (Linux сервер)

```bash
# Загрузите файлы на сервер и запустите:
./auto_deploy.sh
```

**Что происходит автоматически:**
- ✅ Останавливает старые процессы
- ✅ Копирует файлы
- ✅ Устанавливает systemd сервис
- ✅ Включает автозапуск
- ✅ Запускает бота
- ✅ Проверяет статус

### Вариант 2: Docker

```bash
# На сервере с Docker:
./deploy_docker.sh
```

**Что происходит автоматически:**
- ✅ Останавливает старые контейнеры
- ✅ Собирает Docker образ
- ✅ Запускает контейнер
- ✅ Включает автоперезапуск
- ✅ Проверяет статус

### Вариант 3: Удаленный деплой

```bash
# С локальной машины на сервер:
./deploy_to_server.sh
```

**Что происходит автоматически:**
- ✅ Подключается к серверу
- ✅ Копирует файлы
- ✅ Запускает деплой
- ✅ Проверяет статус

## 🎯 РЕЗУЛЬТАТ:

**БОТ РАБОТАЕТ АВТОНОМНО:**
- 🤖 Без вашего участия
- 🔄 Автоматический перезапуск при сбоях
- 🚀 Автоматический запуск при перезагрузке сервера
- 📱 Отвечает на сообщения в Telegram
- 🧠 Анализирует встречи через Gemini AI
- 📊 Обновляет лиды в Bitrix24

## 📋 Файлы для деплоя:

- `autonomous_bot.py` - сам бот
- `meeting-bot.service` - systemd сервис
- `auto_deploy.sh` - автоматический деплой
- `deploy_docker.sh` - Docker деплой
- `deploy_to_server.sh` - удаленный деплой
- `docker-compose.yml` - Docker конфигурация
- `Dockerfile` - Docker образ

## 🚨 ВАЖНО:

**ВСЁ ПРОИСХОДИТ АВТОМАТИЧЕСКИ!**
**НИКАКОГО ВАШЕГО УЧАСТИЯ НЕ ТРЕБУЕТСЯ!**
**БОТ РАБОТАЕТ 24/7 НА СЕРВЕРЕ!**
